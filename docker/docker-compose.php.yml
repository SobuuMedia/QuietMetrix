services:
  php:
    build:
      context: ..
      dockerfile: servers/php/Dockerfile
    ports:
      - "8080:80"
    environment:
      QM_PROFILE: selfhost
      QM_DB_HOST: mysql
      QM_DB_NAME: quietmetrix
      QM_DB_USER: quietmetrix
      QM_DB_PASSWORD: quietmetrix
      QM_JWT_SECRET: change-me-in-production
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  mysql:
    image: mysql:8
    environment:
      MYSQL_DATABASE: quietmetrix
      MYSQL_USER: quietmetrix
      MYSQL_PASSWORD: quietmetrix
      MYSQL_ROOT_PASSWORD: rootpassword
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - php
    restart: unless-stopped

volumes:
  mysql_data:
  caddy_data:
  caddy_config: