services:
  ktor:
    build:
      context: ..
      dockerfile: servers/ktor/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      QM_PROFILE: selfhost
      QM_DB_URL: jdbc:postgresql://postgres:5432/quietmetrix
      QM_DB_USER: quietmetrix
      QM_DB_PASSWORD: ${QM_DB_PASSWORD:?QM_DB_PASSWORD must be set in docker/.env}
      QM_JWT_SECRET: ${QM_JWT_SECRET:?QM_JWT_SECRET must be set in docker/.env}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    env_file:
      - .env
    environment:
      POSTGRES_DB: quietmetrix
      POSTGRES_USER: quietmetrix
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in docker/.env}
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quietmetrix"]
      interval: 5s
      timeout: 5s
      retries: 5

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - ktor
    restart: unless-stopped

volumes:
  pg_data:
  caddy_data:
  caddy_config: